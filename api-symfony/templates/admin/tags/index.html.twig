{% extends 'admin/layout.html.twig' %}

{% block title %}Tags Management{% endblock %}

{% block content %}
    <div class="content-wrapper">
        <section class="content-header">
            <h1>
                Tags Management
                <small>list of all tags</small>
            </h1>
            <ol class="breadcrumb">
                <li><a href="{{ path('admin_dashboard') }}"><i class="fa fa-dashboard"></i> Home</a></li>
                <li class="active">Tags</li>
            </ol>
        </section>

        <section class="content">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Tags Listing</h3>
                </div>
                <div class="box-body">
                    {% for message in app.flashes('success') %}
                        <div class="alert alert-success alert-dismissible">
                            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                            <p>{{ message }}</p>
                        </div>
                    {% endfor %}

                    <div class="form-group">
                        <a href="{{ path('admin_tags_create') }}" class="btn btn-success">Add New</a>
                    </div>

                    <table id="admin_data_table" class="table table-bordered table-striped">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for tag in tags %}
                            <tr>
                                <td>{{ tag.id }}</td>
                                <td>{{ tag.title }}</td>
                                <td>
                                    <a href="{{ path('admin_tags_edit', {id: tag.id}) }}">
                                        <i class="fas fa-pencil-alt"></i>
                                    </a>
                                    <form method="POST" action="{{ path('admin_tags_delete', {id: tag.id}) }}"
                                          style="display: inline;">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete_tag_' ~ tag.id) }}">
                                        <button onclick="return confirm('Are you sure?')" type="submit" class="delete"
                                                style="background:none;border:none;color:#337ab7;cursor:pointer;">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3" class="text-center">
                                    No tags found. <a href="{{ path('admin_tags_create') }}">Create your first tag</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
