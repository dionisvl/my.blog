{% extends 'admin/layout.html.twig' %}

{% block title %}Portfolio{% endblock %}

{% block content %}
    <div class="content-wrapper">
        <section class="content-header">
            <h1>
                Portfolio
                <small>work showcase</small>
            </h1>
        </section>

        <section class="content">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Portfolio Entries</h3>
                </div>
                <div class="box-body">
                    <div class="form-group">
                        <a href="{{ path('admin_portfolios_create') }}" class="btn btn-success">Add Entry</a>
                    </div>
                    <table id="admin_data_table" class="table table-bordered table-striped">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Image</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for portfolio in portfolios %}
                            <tr>
                                <td>{{ portfolio.id }}</td>
                                <td>{{ portfolio.title }}</td>
                                <td>
                                    <img src="{{ portfolio.imagePath }}" alt="" width="100">
                                </td>
                                <td>
                                    <a href="{{ path('admin_portfolios_edit', {id: portfolio.id}) }}">
                                        <i class="fas fa-pencil-alt"></i>
                                    </a>
                                    <form method="POST"
                                          action="{{ path('admin_portfolios_delete', {id: portfolio.id}) }}"
                                          style="display: inline;">
                                        <input type="hidden" name="_token"
                                               value="{{ csrf_token('delete' ~ portfolio.id) }}">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <button onclick="return confirm('Are you sure?')" type="submit" class="delete"
                                                style="background:none;border:none;color:#337ab7;cursor:pointer;">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
