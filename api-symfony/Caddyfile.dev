{
    frankenphp {
                   php_ini memory_limit 256M
                   max_threads auto
               }

    order php_server before file_server
}

:80 {
    root * /app/public
    encode zstd gzip

    php_server
}
